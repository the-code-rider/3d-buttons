{
  "name": "3D Buttons Everywhere",
  "description": "Adds a smooth, three-dimensional hover/press effect to all clickable elements on every page.",
  "version": "1.0",
  "manifest_version": 3,

  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "css": ["styles.css"],
      "js":  ["contentScript.js"],
      "run_at": "document_idle"
    }
  ]
}

